{
  "name": "sf-symbols-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate": "node ./scripts/generate-icons-data.js",
    "build": "rm -rf dist && npm run build --prefix .. && mkdir -p dist && cp -R ../dist/* dist/ || true && cp index.html dist/index.html",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {}
}